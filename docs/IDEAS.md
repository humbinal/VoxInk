**注意：**

- 所有开发直接在 main 分支开发，每开发完成一个 进行 git commit
- 涉及到UI显示问题的，修改后先用 ui-verify skill 验证效果才能证实确实修改可靠

## 当前缺陷情况：

- [x] 设置弹窗，窗口下侧两边的圆角边框渲染不正常
- [x] 设置弹窗界面 ASR 服务 的下拉选择没有多语言，通用配置中将语言切为 English ，下拉选择依旧为中文， 数据
  配置项中的存储位置提示，也存在该问题
- [x] 录音状态是的波形图，对音量的感知不明显，需要考虑参考 adobe audition 类似的波形图或算法，使其对音量的感知更敏感

## 新特性规划:

- [x] 设置菜单的左侧名称 全局快捷键 改为 快捷键， 在右侧内容区通过分节 显示 全局快捷键 和 应用内快捷键，快捷键 后方增加
  是否已经冲突的提示，用户设置时如果存在冲突，也需要提示处理

- [ ] 品牌图标重新设计，app主图标、系统任务栏图标、托盘图标一致性设计，系统任务栏和图盘图标加入录制状态、转录状态等优雅设计

- [ ] 加入转录后AI润色，涉及到模型配置（主流厂商+OpenAI兼容接口），各种场景的润色模板（默认+修改+自定义模板），这就需要在主界面增加
  AI润色 按钮，同时要考虑润色后的内容怎么显示，是直接替换就内容还是split两个窗口，左右分别查看。如果直接替换原始内容就丢了，没法找回只能重新转录了，代价较大；

- [ ] 录音片段条目的重新转录，需要有状态，比如按钮改为等待转圈，此时播放和删除需要禁用；

- [ ] 录音片段条目的播放改为直接在应用内实现，播放时按钮切换为暂定操作符；自动播放下一条片段；全部片段播放完毕后停止；播放时最好有进度条，可以在整个条目的背景上实现进度条，整条代表100%，然后不同的前景色从头到尾扫过整条；

- [ ] 进一步的极致交互体验，mini窗口(类似输入法状态条那种，置顶单行小组件)
  ，显示当前录制状态/时长/转录文字数量/操作开始、停止等，可能会比较复杂，需要先分析实现方案，实现代价，副作用等，然后让我决定是否开展；
